import { Menu as PhoneMenu, PhoneFooter } from "../../components";

const Menu = () => {
  return (
    <div className="relative">
      <PhoneMenu />
      <PhoneFooter />
    </div>
  );
};

export default Menu;
